name: dsp-game
channels:
  - pytorch
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - numpy
  - pip
  - pip:
      - prefect
      - librosa
      - psycopg[binary]
      - torch
      - torchaudio
      - python-dotenv